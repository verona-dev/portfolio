---
import type { HTMLAttributes } from "astro/types";
import { tv, type VariantProps } from "tailwind-variants";

interface Props extends HTMLAttributes<"div">, VariantProps<typeof avatar> {}

const avatar = tv({
	base: "text-foreground bg-muted relative overflow-hidden rounded-full border-2",
	variants: {
		variant: {
           custom: "border-none",
			default: "border-border",
			primary: "border-primary",
			secondary: "border-secondary",
			info: "border-info",
			success: "border-success",
			warning: "border-warning",
			error: "border-error",
		},
		size: { sm: "h-8 w-8 text-xs", md: "h-10 w-10 text-sm", lg: "h-12 w-12 text-base", custom: "w-72 h-72 lg:w-96 lg:h-96 lg:self-center text-xl" },
	},
	defaultVariants: { variant: "custom", size: "md" },
});

const { variant, size, class: className, ...rest } = Astro.props;
---

<figure class={avatar({ variant, size, class: className })} {...rest}>
	<slot />
</figure>
